@echo off
title T-regressor Prediction
cd /d "%~dp0"
if "%~1" == "" (
    echo CSVファイルをこのアイコンにドラッグ^&ドロップしてください。
    pause
    exit
)
echo ===================================================
echo   T-regressor 予測ロボット
echo ===================================================
echo.

if exist predict_native.exe (
    predict_native.exe "%~1"
    if not errorlevel 1 goto :done
    echo.
    echo [警告] ネイティブ版が使用できないため Python で実行します...
    echo.
)

python predict.py "%~1"

:done
echo.
echo ===================================================
echo 完了！  _predicted.csv が保存されました。
echo ===================================================
pause
