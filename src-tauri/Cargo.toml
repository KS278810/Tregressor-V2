[package]
name = "t-regressor"
version = "1.0.0"
edition = "2021"

[lib]
name = "t_regressor_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }
zip = { version = "2.1", default-features = false, features = ["deflate"] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rfd = "0.15"
zip = { version = "2.1", default-features = false, features = ["deflate"] }
encoding_rs = "0.8"
